/build
/.svelte-kit
/package
.env
.env.*
!.env.example
# Unignored: a public hostname, and app.html's preconnect hints substitute an unset
# variable to an empty string rather than failing the build.
!.env.development
!.env.production
!.env.staging
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Installed Node.js modules
node_modules/

# Build directory for Vercel deployments
.vercel_build_output

# Target build directory for processed, compiled web app
dist/

.tmp
test-results/
playwright-report/
blob-report/

# SASS intermediate compile files
*.scssc

# SASS cache directory
.sass-cache

# temporary editor save files
*~

# OSX thumbnail caches
.DS_Store

# log files
*.log
npm-debug.log*

# JetBrains editor dir
.idea/


# Coverage reports
coverage/

# Vitest browser-mode failure screenshots and attachments
.vitest

# Production logs: the dumps hold visitor IP addresses and the database is derived
# from them. Only the SQL and the fixtures are tracked.
production_logs/dumps/
*.duckdb
*.duckdb.wal
*.duckdb.building
*.duckdb.building.wal
*.duckdb.lock
